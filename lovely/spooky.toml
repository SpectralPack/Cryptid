[manifest]
version = "1.0.0"
dump_lua = true
priority = 10

# Haunted House - useless skips
[[patches]]
[patches.regex]
target = "functions/button_callbacks.lua"
pattern = 'if _tag then'
position = 'at'
line_prepend = '$indent'
payload = '''if _tag and not G.GAME.events.ev_cry_choco2 then'''


# Revered Antique - create legendary
[[patches]]
[patches.pattern]
target = "game.lua"
pattern = '''if G.load_shop_booster then'''
position = "before"
payload = '''
if G.GAME.events.ev_cry_choco10 then
    local add = true
    for k, v in pairs(G.shop_vouchers.cards) do		-- G.load_shop_vouchers is already set to nil here, just do a normal check
        if v.ability.cry_antique then add = false end
    end
    if add then
        local card = create_card('Joker', G.jokers, true, nil, nil, nil, nil, 'cry_antique')
        Cryptid.manipulate(card)
        card.misprint_cost_fac = 50/card.cost
        card:set_cost()
        create_shop_card_ui(card, 'Voucher', G.shop_vouchers)
        card:start_materialize()
        card.ability.cry_antique = true
        G.shop_vouchers.config.card_limit = G.shop_vouchers.config.card_limit + 1
        G.shop_vouchers:emplace(card)
    end
end
'''
match_indent = true

# Monopoly Money - don't use card if destroyed by Monopoly Money
[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = '''if e.config.id == 'buy_and_use' then'''
position = "at"
payload = '''if e.config.id == 'buy_and_use' and not (c1 and c1.cry_monopoly_destroyed) then'''
match_indent = true

# Monopoly Money - don't use card in use_card if destroyed by Monopoly Money
[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = '''local card = e.config.ref_table'''
position = "after"
payload = '''if card and card.cry_monopoly_destroyed then return end'''
match_indent = true
